# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.pytest_tmp/
*.egg

# Local runtime / data
runs/
models/
temp/
*.db
*.sqlite
config/ui_settings.json
config/prompts/

# Archives / unpack leftovers
VulnForge-main/
VulnForge-main.zip
*.zip

# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp

# Large runtime deps live at repo root for local/offline packaging.
# They are NOT always committed (size); use scripts/package_offline_release.ps1
# to ship a full offline zip that includes them.
# Leading slash = root only so vulnforge/ghidra/ (Python client) stays tracked.
/ghidra/
# Vendored MCP source may be committed; ignore rebuildable/local junk only:
/ghidra-mcp/.venv/
/ghidra-mcp/.gradle/
/ghidra-mcp/build/
/ghidra-mcp/logs/
/ghidra-mcp/.git/
# Offline release artifacts
/dist/*.zip

# Unrelated siblings in this workspace (not part of VulnForge package)
katie/
ornith.py
ornith_*.py
ornith_*.sh
grok-ornith.sh
start_ornith_server.sh
download_models.sh
benchmark_ornith.py
benchmark_results.json
OPTIMIZATION_GUIDE.md
README_SERVER.md
requirements.txt
